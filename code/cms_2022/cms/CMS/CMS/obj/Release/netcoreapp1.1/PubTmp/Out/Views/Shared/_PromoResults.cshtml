@model PromosView

<table class="table table-bordered table-hover table-striped" id="results-table" data-size="@Model.PromoListing.Options.Results.pageSize" data-sortfield="@Model.PromoListing.Options.Results.sortField" data-sortorder="@Model.PromoListing.Options.Results.sortOrder" data-searchfield="@Model.PromoListing.Options.Results.searchField" data-searchvalue="@Model.PromoListing.Options.Results.searchValue">
    <thead>
        <tr>
            <th class="btnsort" data-field="Name" data-order="asc">Promo Name</th>
            <th class="tw-large btnsort" data-field="Created" data-order="asc">Date Created</th>
            <th class="tw-large btnsort" data-field="Published" data-order="asc">Last Published</th>
        </tr>
    </thead>
    <tbody>
        @if (Model.PromoListing.Promo.Count > 0)
        {
            foreach (var item in Model.PromoListing.Promo)
            {
            <tr>
                <td>@item.Name</td>
                <td>@DateTime.ParseExact(item.DateCreated.ToString(), "yyyyMMddHHmmssffff", null).ToString("dd-MM-yyyy HH:mm tt")</td>
                <td><a href="~/promos/details?promo=@item.Id">@DateTime.ParseExact(item.DatetimeModified.ToString(), "yyyyMMddHHmmssffff", null).ToString("dd-MM-yyyy HH:mm tt")</a></td>
            </tr>
            }
        }
        else
        {
        <tr>
            <td colspan="3">No promos were found</td>
        </tr>
        }
    </tbody>
</table>
<!-- /.table-responsive -->
@Html.Partial("_Pagination", Model.PromoListing.Options.Results)